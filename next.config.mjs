const nextConfig = {
    // Your Next.js configuration
    // Ensure that the environment supports ES modules
    experimental: {
      esmExternals: true,
    },
  }
  
  export default nextConfig
  